"""Root conftest.

Placing a conftest at the repository root puts this directory on
``sys.path`` during test collection, so test modules can import source
packages (``foundations``, ``algorithms``, ...) without installation.
"""
